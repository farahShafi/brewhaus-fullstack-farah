# Auth0 / OAuth config
AUTH_DOMAIN=your-auth0-domain
CLIENT_ID=your-client-id

# (Optional for non-PKCE flows, not needed here but safe to keep)
# CLIENT_SECRET=your_client_secret_here

# App config
PORT=3000

# JWT (if you verify tokens yourself)
JWT_SECRET=super_secret_key